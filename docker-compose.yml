# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - /data/db


  handle-services:
    image: bencegyurus/handle-server:lastes
    ports:
      - 3000:3000