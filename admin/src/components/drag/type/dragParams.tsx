type typeOfDragParams = {
    width : number,
    height : number,
    style? : React.CSSProperties,
    startFunction? : Function,
    onDragFunction? : Function,
    endFunction? : Function
}

export default typeOfDragParams;