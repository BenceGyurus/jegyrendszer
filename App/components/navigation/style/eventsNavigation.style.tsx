import { StyleSheet } from "react-native";

const EventNavigationStyle = StyleSheet.create({
    eventNavigationScrollView : {
        width : "100%",
        height : "105%"
    }
});

export default EventNavigationStyle;